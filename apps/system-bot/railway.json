{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build -w @opus/core && npm run build -w @opus/general-bot"
  },
  "deploy": {
    "startCommand": "npm run start -w @opus/general-bot",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
